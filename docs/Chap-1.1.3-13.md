## Fixed

- **Correct reused-window placement** — Chap now preserves the Chrome window
  selected by URL reuse instead of reactivating Chrome and resizing a different
  previously focused window.

## Notes

- macOS 14.0+ required. Chrome required for URL launch type.
