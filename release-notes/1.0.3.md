## What's New

- **Import format reference** — Failed or blocked imports now show a "Show Expected Format" button with field requirements, an example JSON config, and a Copy button.
- **App launcher observation refactor** — Extracted testable eligibility predicates (`canResizeNewWindow`, `shouldAttemptEarlyFocusedFallback`, `isGracePeriodExpired`) from the AX observation loop for improved clarity and test coverage.

## Improved

- Landing page now links both the PKG primary installer and a DMG manual-install fallback.
- Release automation bumps all version surfaces (README examples, footer, download URLs) atomically.
